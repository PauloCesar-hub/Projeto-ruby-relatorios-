
<section class="card">
  <h2>Gerar relatório</h2>
  <form id="report-form" method="post" enctype="multipart/form-data">
    <div class="grid">
      <div>
        <label>Arquivo (.csv ou .json)</label>
        <input type="file" name="file" />
      </div>
      <div>
        <label>OU URL de API (JSON)</label>
        <input type="url" name="url" placeholder="https://api.exemplo.com/despesas" />
      </div>
      <div>
        <label>Tipo de relatório</label>
        <select name="type">
          <option value="expenses">Despesas</option>
          <option value="notes">Notas</option>
        </select>
      </div>
      <div>
        <label>Campos (opcional, separados por vírgula)</label>
        <input type="text" name="fields" placeholder="ex: date,category,amount" />
      </div>
      <div>
        <label>Data inicial (opcional)</label>
        <input type="date" name="from" />
      </div>
      <div>
        <label>Data final (opcional)</label>
        <input type="date" name="to" />
      </div>
    </div>
    <div class="actions">
      <button type="submit" formaction="/export/csv">Baixar CSV</button>
      <button type="submit" formaction="/export/pdf">Baixar PDF</button>
      <button type="submit" formaction="/export/xlsx">Baixar XLSX</button>
    </div>
    <p class="muted"><small>Dica: use os exemplos em <code>/samples</code>.</small></p>
  </form>
</section>

<section class="card">
  <h2>Exemplos de dados</h2>
  <ul>
    <li><a href="/samples/expenses.csv" download>samples/expenses.csv</a></li>
    <li><a href="/samples/notes.json" download>samples/notes.json</a></li>
  </ul>
</section>
